# Port for local SQL Server mapping
MSSQL_PORT=1433

# Password for the SQL Server SA account. Must meet SQL Server password policy requirements (e.g., at least 8 characters, containing uppercase, lowercase, numbers, and/or special characters).
MSSQL_SA_PASSWORD=YourStrong@Password123
